<div class="inside">
  <div class="row">
    {{> card}}
    <div class="col s12 m12 l5" id="right">
      <div class="mini-nav center">
        <a href="/profile/{{user._id}}"><button class="btn-small black">Profile</button></a>
        <a href="/profile/{{user._id}}/portfolio"><button class="btn-small black">Portfolio</button></a> 
        <a href="/profile/{{user._id}}/skills"><button class="btn-small black">Skills</button></a>
        <a href="/profile/{{user._id}}/rate/edit"><button class="btn-small blue darken-3">Edit $ Rate</button></a>
      </div>
      <h3>Rate</h3>
      <form disabled>
      <div class="input-field"><label>Per Hour Rate ($)</label>
      <input type="number" name="rate" value="{{user.rate}}" disabled></div>
      </form>
    </div>
  </div>
</div>